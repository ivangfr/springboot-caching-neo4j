spring:
  application:
    name: restaurant-api

## You can enable persistence for the embedded mode by providing a path to a database file in your configuration
## ---
#  data:
#    neo4j:
#      uri: file://${user.dir}/target/neo4j-embedded/graph.db